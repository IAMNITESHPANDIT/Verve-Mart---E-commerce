import { Container } from "react-bootstrap";

interface iDash {
  data: [];
}

const Dashboard: React.FC = ({}) => {
  return (
    <Container>
      <h2>Dashboard dev</h2>
    </Container>
  );
};

export default Dashboard;
